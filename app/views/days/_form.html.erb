<%= form_with(model: day, local: true) do |form| %>
  <% if day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
      <% day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :foods_eaten %>
    <%= form.text_area :foods_eaten %>
  </div>

  <div class="field">
    <%= form.label :symptoms %>
    <%= form.text_area :symptoms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
